# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=512m
org.gradle.daemon=false
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# ============================================================================
# 1.21.11 backport build pins (WP-12111-1). Evidence: docs/records/PORT_12111_GATES.md
# ============================================================================

# Parchment (1.21.11 IS obfuscated; mappings are required again).
# Pinned values copied from the official MDK-1.21.11-ModDevGradle (fetched 2026-08-22):
#   parchment_minecraft_version=1.21.11 / parchment_mappings_version=2025.12.20
parchment_minecraft_version=1.21.11
parchment_mappings_version=2025.12.20

# Environment Properties
# Latest NeoForge 21.11.x (release channel) — pinned from the official MDK-1.21.11-ModDevGradle
# (fetched 2026-08-22: neo_version=21.11.45). NeoForge official news "NeoForge 21.11 for
# Minecraft 1.21.11" (2025-12-09) confirms the 21.11 line targets obfuscated 1.21.11.
# The Minecraft version must agree with the Neo version to get a valid artifact.
minecraft_version=1.21.11
# The Minecraft version range can use any release version of Minecraft as bounds.
minecraft_version_range=[1.21.11]
# The Neo version must agree with the Minecraft version to get a valid artifact.
neo_version=21.11.45

# Optional recipe-viewer compile artifacts (1.21.11 line). Version evidence: sister project
# TaCZ_Refabricated_Unofficial 1.21.11 branch gradle.properties + docs/PORT_1_21_11_PHASE1.md
# (all resolved against live artifact repos on 2026-08-13, Fabric variants). The NeoForge
# variants below are same-line best-effort pins — each dependency in build.gradle has a libs/
# escape hatch; verify with HTTP 200 on a networked machine (see PORT_12111_GATES.md).
jei_neoforge_version=27.23.0.71
# REI 1.21.11 line (21.11.816, sister fabric pin). libs/-ONLY: the NeoForge artifact is
# not on maven under this version (user build log 2026-08-22) — download the 1.21.11
# NeoForge jar from Modrinth into libs/ (see build.gradle REI block).
rei_neoforge_version=21.11.816
rei_architectury_version=19.0.1
rei_cloth_config_version=21.11.153
# ZigyTheBird Player Animation Library — 1.21.11 line is 1.1.9 (26.1.2 used 1.2.5; API delta
# -> compat/playeranimator/** must be re-checked, sister project phase-1 table says the same).
player_animation_lib_version=1.1.9
# MrCrayfish Controllable 1.21.11-0.25.8 — libs/-ONLY: Modrinth maven does not serve the
# NeoForge jar under this version (user build log 2026-08-22). Exact verified source:
#   https://github.com/MrCrayfish/Controllable/releases/download/v1.21.11-0.25.8/controllable-neoforge-1.21.11-0.25.8-signed.jar
controllable_neoforge_version=1.21.11-0.25.8
# Exopandora Shoulder Surfing Reloaded — 1.21.11-5.0.10 line (sister pin: fabric 5.0.10).
shoulder_surfing_neoforge_version=1.21.11-5.0.10+neoforge
# Iris — 1.10.7+mc1.21.11 line (sister pin: fabric 1.10.7+mc1.21.11, curse 7805348).
# Client-dev runtime profile only; Iris mixins use string targets, no compile dependency.
iris_neoforge_version=1.10.7+mc1.21.11-neoforge
# Carry On — 2.9.2 line (sister R2 pin). Mixins use string targets, no compile dependency;
# runtime compat work follows sister R2 (docs/CARRYON_COMPAT.md) later.
carryon_neoforge_version=2.9.2

## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
# Keep as "tacz" so existing gun packs' >=1.1.8 dependency checks still resolve.
mod_id=tacz
# The human-readable display name for the mod.
# Brand: "TaCZ: Renovated" (unofficial NeoForge port; naming record: docs/records/NAMING_DECISION.md).
# The modId below must stay "tacz" forever — gun pack dependency checks resolve against it.
mod_name=TaCZ: Renovated
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=GPL-3.0-only
# The mod version. See https://semver.org/
#
# SemVer trap (charter 7.4): MUST use '+' build metadata, NEVER '-' pre-release.
# Gun packs check >=1.1.8; "1.1.8-neoforge..." is a pre-release and sorts BEFORE 1.1.8,
# so the check silently fails. "1.1.8+neoforge..." is build metadata and still satisfies >=1.1.8.
mod_version=1.1.8+neoforge.1.21.11.R0
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=com.tacz.guns

# 1.21.11 IS obfuscated (NeoForge 21.11 release notes, 2025-12-09). Mappings + mixin refmap are
# required again — build.gradle follows the MDK-1.21.11 canonical form (neoForge { parchment {} }).
# The 26.1.2 note "Do not add mappings/parchment" no longer applies.
