# 联机实测记录 #5 —— 2026-08-21 枪包装载专项（L2.5）

> 冻结快照。log：`main(26.1.2)` commit fad01f0 `latest.log`（专服，4 枪包）。

## A. 双端安装（正常路径）：PASS

- 服务端装载 4 包：`guns=253 ammo=44 attachments=145 blocks=9 recipes=441`
  （默认包 54/24/99/3 + 三方包增量），两轮 reload 计数一致；
- **全程零错误**：无 NoClassDefFoundError / 空栈编码 / BLOCK_INDEX / RECIPE_FILTER
  解析错误——Beta-2 同步修复在 4.7 倍枪械量下回归通过；
- 用户实测三判据全 PASS：`/give` 包内枪不崩且译名正确、开枪有伤害有模型音效、
  包内配方可在工作台合成；
- 在线 `/tacz reload`（21:12:59，玩家 GOOSTL 执行）：重载干净、无踢出。

## B. 不对称安装：不崩（底线成立），行为已确认

| 场景 | 实测表现（用户确认） |
|---|---|
| 只装客户端 | **无事发生**——服务端不认包内 id；服务端补装后 `/tacz reload` 即正确加载可用 |
| 只装服务端 | 紫黑方块 + 名字只剩 `....name` 原始翻译键（语言文件/模型在客户端资产里，不走网络同步）；不崩、不踢 |

## C. 关键行为发现：客户端本地新增包不支持热加载

- 客户端在**游戏进程运行中**向 `.minecraft/tacz/` 新增包：`/tacz reload` **无效**
  （该命令在专服环境只重载服务端并重同步 common 数据；客户端本地包的发现
  发生在客户端资源包扫描时）。
- **已确认的生效方式：重启客户端进程。**
- 待验证假说：F3+T（客户端资源重载会重跑 pack finder、重扫 `tacz/` 目录），
  理论上应能免重启生效——下次测试顺手验证，成立则更新本行与 README。

## 结论

- L2.5 全部判据关闭；不对称行为从"预期方向"转为确认事实，已回写预案与 README。
- 无新增代码缺陷；C 为架构行为（上游同构），按文档化处理，不立修复项。
