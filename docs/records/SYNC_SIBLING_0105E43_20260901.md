# 姊妹线 `arena/01a05e43` 同步记录（2026-09-01，NeoForge 26.2 线）

> 方向：姊妹线 `q14433686-arch/TaCZ_Renovated` 分支
> `arena/01a05e43-tacz-renovated`（NeoForge 1.21.11，同步前 tip `5fa0963`）
> → 本仓 `26.2`（NeoForge 26.2.0.64，同步前 tip `fe3e097`）。
>
> 核对方式：逐 commit 读姊妹线 diff（`git show <sha>` 全文），与本线文件逐行比对后再移植；
> 姊妹线 `41319d7` 的 7 个落地项在本线逐条复算「是否已存在 / 以何种形态存在」，
> 证据级别：**静态（读码）+ 逐字节 diff 旁证**（动画三文件已 md5 比对确认逐字节相同）。
> 本线 CI 编译门与实机**均未跑**（沙箱无 JDK），按 AGENTS §2 本记录不写「已修好」，
> 只写「已移植、待编译/实机验证」。

## 1. 落地项

| 姊妹线条目 | 本线改动 |
|---|---|
| P0-b 纯查表（`c9b8ba1`，姊妹 `41319d7` 搬运） | `ClientAttachmentItemTooltip` / `ClientBlockItemTooltip` 弃 `I18n.get` 的格式化语义，改 `Language.getInstance().getOrDefault(...)`（26.2 类名 `net.minecraft.locale.Language`，与本线 `PapiManager` 一致）。`PapiManager` 本线已在 `ec51f556` 修过（姊妹线同条注释亦指认 26.2 为源），代码与姊妹 tip **逐字节等价**，未动。 |
| `tacz:nbt` 跨包材料（Fabric 线 `e1aad10` 第 1 件，姊妹 `41319d7` 按 1.21.11 原生管线改写） | `RecipeCompat`：`SUPPORTED_FORGE_INGREDIENTS` 增 `tacz:nbt`；`items` 单字符串（TaCZPackUpgrader 写形）包成数组；`strict = !partial`（`partial` 缺省 false = 严格，与上游 codec `optionalFieldOf("partial", false)` 一致；本线 `PartialNbtIngredient.CODEC` 实测为 `optionalFieldOf("strict", false)`，改写目标字段名吻合）；`normalizeLegacyIngredient` 的旧无 type `{item + nbt}` 对象不再静默丢 nbt，改写为 `tacz:partial_nbt` 宽松子集语义（partial 是唯一可命中的语义——用过的枪必然带额外字段），改写时 INFO 记一笔。`GunSmithTableIngredient`：catch 面加 `LinkageError`，WARN 带规范化形态（跨包合成排查的第一现场）。 |
| `/tacz overwrite` 显式落盘（Fabric 线 `cd14a2a` 第 4 项，姊妹 `41319d7` 等价搬） | `OverwriteCommand#setOverwrite` 在 `override.set(...)` 后显式 `PreLoadConfig.spec.save()`。本线原生 NeoForge `ModConfigSpec#save()` 即 `loadedConfig.save()`（本线 `MenuIntegration` 的 Cloth `savingRunnable` 早已对 `CommonConfig`/`ClientConfig` 同款调用，API 存在性由本线已编译代码旁证）；`tacz-pre.toml` 的 `DefaultPackDebug` 不再「命令行改完重启回默认」。 |
| lang 补键（姊妹 `41319d7` 补 2 键） | `attribute.name.tacz.bullet_resistance`（en/zh）—— 本线 `ModAttributes` 实际引用，此前属性名显示原始键；`commands.tacz.arguments.enum.invalid`（en/zh）—— 本线 **无** Java 引用（`command/sub` 无一用 enum 参数），仅按三线键表对齐补入，防将来加 enum 参数时缺键；与姊妹线取值逐字一致。 |

## 2. 不搬清单（含理由，防反向污染）

| 姊妹线条目 | 理由（本线现状，读码核实） |
|---|---|
| P0-a functionalTasks 手动 flush（`1cfa42b`+`cb39564`，姊妹 `41319d7`） | **架构差异，非本线缺失。** 姊妹 1.21.11 线把瞄具 body 改成「自己按序重放几何」（`collector.order(...)` 逐份快照 `write`），绕开了 `super.submit`，于是快照里冻结的字体提交任务无人 flush —— 那是他们线特有的病。本线 `BedrockAttachmentModel#submit` 的镜身走 `super.submit(...)` → `BedrockModel#submit` 在 `submitCustomGeometry` 之后无条件 `snapshot.submitFunctionalTasks(collector)`（`BedrockModel.java:381`），任务不丢；`setTextShowList` 已对瞄具文字传 `clipToScopeMask=true`。**刻意偏离（已记录）**：姊妹线对 `ocularSnapshots`/`ocularRingSnapshot` 各多一次 flush 兜底「第三方镜把 text_show 挂在 ocular/镜框子树、而该子树本帧被临时隐藏」的边角；本线 `capturePart` 对**直接是**功能节点的隐藏部件仍会取值（`FunctionalBedrockPart` 分支在 `visible` 判断之前），仅「隐藏普通节点的**更深层**子树」拿不到。按 R2 在途纪律本轮不擅自加「先拍子树快照再只 flush 任务」的新代码面；若第三方包实测报该场景丢字，照姊妹线形状补（captureSubtree 前置 + 任务单 flush）。 |
| P1 镜内文字掩码裁剪全套（`d076cf5`，姊妹 `41319d7`：`ScopeTextSubmitter`/`scope_text_final.fsh`/`ScopeRenderTypes.maskedText`/`ScopeDepthCopyState.isMaskCycleValid`/`TextShowRender.clipToScopeMask`） | 本线**已有同功能、且更完整的独立实现**（26.2 掩码架构）：`ScopeTextSubmitter`（229 行，含姊妹 183 行版没有的 `submitOrdered(OrderedSubmitNodeCollector)` 入口 —— 26.2 两收集器为平级类型）、`ScopeTextRenderTypes`（`clippedText`/`finalText` 双管线副本，后者不交 Iris）、`shaders/core/scope_text_final.fsh`、`TextShowRender` 的 `clipToScopeMask` 四参构造 + 瞄具侧 `true`/枪身侧 `false`、光影下 `ScopeFinalRingOverlay.queueText` 延迟路径。失败语义同构：掩码未就绪 ⇒ 回退 vanilla `submitText`，不丢字、不画错。 |
| 动画两修（`1c765c6`，姊妹注明「= 26.2 `4aa8d7b`+`12d6f3c`」） | 本线即源头。`ObjectAnimationRunner`/`AnimationStateContext`/`AnimationStateMachine` 三文件与姊妹 tip **md5 逐字节相同**（已比对）。 |
| 81dfb50「幽灵使用」/耳鸣资源/音效失败可见（`UsePressGate`/`MinecraftUseRestartMixin`/`StuckUseRecovery`/`sounds.json`/`stun_ringing.ogg`/效果图标/`DeafenState` PlayResult WARN） | 姊妹 `81dfb50`（及其 PR #24 合并）的全部文件本线已逐件在位：`client/input/UsePressGate`、`client/input/StuckUseRecovery`、`mixin/client/MinecraftUseRestartMixin`、`DeafenState`、`assets/lrtactical/sounds.json`、`sounds/stun_ringing.ogg`、`textures/mob_effect/{deafened,blinded}.png` 均存在（`scripts/verify_lr_assets.py` 可自检），无差异可搬。 |
| 姊妹 `a00babe` 的 `.github/workflows/compile-check.yml`（+ `5fa0963` ci-log 流程） | CI 门禁属仓库所有者人工流程：本线 `.github/workflows/` 现有 `compile-check.yml`/`consistency.yml`（`docs/ci/` 有存档），**本轮不动 workflow**，按发起人指示等人工跟进。 |
| 姊妹 `3a8bc4f` 回执文档、`CHANGELOG` 的 1.21.11 版本号表述 | 对方线内务；本线用本记录 + 本线 CHANGELOG `Unreleased` 条目对账。 |

## 3. 继承的已知限制（与姊妹线 `41319d7` 记录 §3 同构，非本轮新引入）

1. 字体页壳缓存（`ScopeTextSubmitter.PAGE_IDS`）在资源包 F5 重载后旧 view 残留为闲置壳
   （`pageId` 注释已写明「序号永不复用、残留无害」），与姊妹线/26.1.2 同源设计。
2. `DisplayMode` 只走 NORMAL 子集（瞄具文字只用 NORMAL），同姊妹线。
3. 姊妹线记录 §3.1 的「文字-only 延迟队列」限制在本线对应形态：仅文字无准星排队的边角
   下，光影延迟路径（`ScopeFinalRingOverlay.queueText`）的画面上限与掩码可用性解耦 ——
   与姊妹线 BACKUP 闸 `hasPendingReticles()` 的问题同构；姊妹线若改闸请同步语义（见其 §5.3，
   本线无 PIP、无独立 BACKUP 闸，触发条件更窄）。

## 4. 待验证（按共用核心 §6 实机清单，未跑前不得宣称已修）

- **tooltip 纯查表**：含 `%` 的内联 `tooltip_key` 枪包，附件/方块 tooltip 不再出现
  `Format error: ...`（对照 `ec51f556` 的 text_show 判据）。
- **`tacz:nbt` 材料**：用被 TaCZPackUpgrader 升级过的枪包（`items` 为单字符串、`partial` 缺省）
  实测合成表材料格显示与匹配；strict/partial 两档各测一次；旧无 type `{item+nbt}` 配方
  材料格不再退化成裸枪（看 `Rewrote a legacy no-type NBT ingredient` INFO 行）。
- **`/tacz overwrite`**：执行后重启，`tacz-pre.toml` 的 `DefaultPackDebug` 保持。
- **lang**：bullet_resistance 属性在属性界面显示「Bullet Resistance / 子弹抗性」而非原始键。
- **本线 CI 编译门未跑**（沙箱无 JDK）；`RecipeCompat` 改写逻辑已用 13 组 JSON 用例
  独立模拟全 PASS（Python 1:1 复刻两方法），但 Java 编译需过 CI 才算闭环。

## 5. 需要回给姊妹线的话

1. 本轮已消费你们 `41319d7`（含 P0-a/P0-b/P1/动画/tacz:nbt/overwrite/lang 七项）的
   本线侧对账：P0-a 与 P1 因本线架构（`super.submit` 自带 flush + 更完整的
   `ScopeTextSubmitter`/`ScopeFinalRingOverlay` 延迟路径）判为「已有等价或更完整实现」，
   不搬；其余五项等价落地（见 §1 表）。
2. 你们 §1.2 的「刻意偏离」与本线判据一致（本线有 PIP、有原生 ModConfigSpec、
   无 meshloader —— 与你们线恰好互补），交叉印证成立。
3. 你们 §3.1 的 BACKUP 闸问题：本线同构点见本档 §3.3，若你们并入
   `hasPendingText()` 改闸，请同步一句，本线 `ScopeFinalRingOverlay` 的队列闸
   （`queueText` 消费者侧）会跟随评估。
4. 本线 CI 门禁（compile-check.yml 等 `.yml`）按仓库所有者流程由人手动跟进，
   本轮不复制你们的 workflow 文件。
