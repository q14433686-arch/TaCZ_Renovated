# 第三轮同步姊妹 `arena/01a04e96`（2026-08-31）

日期：2026-08-31
作者：Arena agent（本仓 `arena/01a04ea3-tacz-renovated`，26.2 / NeoForge）
上一同步点：`9f7412e`（她的 mesh 第 1 步终态；`f01864e` 之后的 `3351f99` 只是 lineage 文档）
本次取货点：`5f36663`（她的分支尖端）
触发：mesh GPU 静态烘焙实机 PASS 后，维护者要求「再核对同步点，看正向的修复和内容是不是全同步了」

> **纪律**：只搬语义，不抄 Fabric 表皮；先取证再动手；未实机一律不写 PASS。
> 本轮额外做了一遍**文件级**对表（不只看提交账本），因为提交账本只能证明「她改过的
> 我都看了」，证明不了「她有而我没有的」——只有比树才能发现后者。

---

## 0. 一句话结论

她自上次同步点只前进了 **2 笔真实提交**（另有 2 笔 ci-log）：

- `c4eb4e2`（镜内文字缺 Iris mode 映射）—— **本仓早就有**（`c692059`），而且我们比她多走一步（`475ea40`）；
- `ec51f55`（`PapiManager` 的 `I18n.get`）—— 同日独立修了，但**类名不同源**，本轮改齐。

文件级对表 35 个共享文件，**除加载器表皮外只有 3 处净差**（2 处注释 + 1 处类名），本轮全部补平。

反向发现两处**本仓领先**：掩码凸包的硬件裁剪（她还在用，我们已判定 26.2 上做不到并撤回）、
光影下镜内文字黑块（她只修了裁切，没修黑块）。这两处要回单给她——见 §5。

---

## 1. 她的两笔增量 × 本仓现状

### 1.1 `c4eb4e2` 镜内文字在光影下不裁 —— 本仓已有，且更进一步

她的修法：`IrisScopeMaskState.resolveModeUncached` 补 `pipeline/scope_text_clipped → mode 2`。

本仓：`IrisScopeMaskState` 第 243 行**早就有**这条映射（`c692059`，2026-08-30，
「文字管线漏登 Iris mode 表」），她的常量块第 30 行才刚加上。同一天的 `475ea40`
还多走了一步——因为**光补 mode 只能救裁切，救不了黑块**：

> 光影包的手部着色器按实体格式取法线与 lightmap，字形被画成黑块。
> 注入分支 `tacz_ScopeMaskMode` 只做 discard、不接管着色。
> 所以光影下的文字改走 `ScopeFinalRingOverlay` 那条最终覆盖路
> （`LevelRenderer#render` 之后、用冻结的手持变换重画），用
> `FINAL_TEXT_PIPELINE`（`pipeline/scope_text_final` + `core/scope_text_final.fsh`），
> 字形 / alpha 裁剪 / 目镜裁剪全部回到我们自己的着色器手里。

她那笔另外两处是**注释纠正**（删掉「光影下掩码整体停用」的旧说法）。
本仓 `ScopeTextRenderTypes` / `ScopeTextSubmitter` 的对应注释**已经是纠正后的版本**，
而且写得更完整（把旧政策为什么错也留了一句）。⇒ 无需再动。

### 1.2 `ec51f55` PapiManager —— 修法一致，类名不同源，本轮改齐

| | 她 | 本仓（改前） | 本仓（改后） |
|---|---|---|---|
| 类 | `net.minecraft.locale.Language` | `…client.resources.language.ClientLanguage` | `net.minecraft.locale.Language` |
| 调用 | `getOrDefault(textKey)` | `getOrDefault(textKey, textKey)` | `getOrDefault(textKey)` |

两条都能编译、语义等价（`ClientLanguage` 是 `Language` 的子类，`getInstance()` 是继承来的静态）。
但**正主是 `net.minecraft.locale.Language`**：我第一轮写的是
`net.minecraft.client.resources.language.Language`，那个类在 26.2 **不存在**——CI 直接
`cannot find symbol`（ci-log(728ca3c) 的实据）。本轮改成与她同名，并把这条类名陷阱
写进 `PapiManager` 的 javadoc，免得下次再踩。

---

## 2. 文件级取证

方法：两边 `src/main/java` 按路径比 md5，再对差异文件做「只留代码行」的 diff。

### 2.1 mesh 包 18 个文件：9 个**字节一致**，6 个有差异

| 文件 | 结论 |
|---|---|
| `api/IPolyMeshBone` `core/BedrockPartBoneAdapter` `core/PolyMeshSupport` `mixin/*`(4) `model/TaczPolyMesh{Ammo,Attachment,Block}Model` | 字节一致（9 个） |
| `core/PolyMesh` `core/PolyMeshModel` | 只有 Fabric `@Environment` 被去掉 |
| `render/PolyMeshGpuRenderer` | 同上，487 行其余逐字节相同 |
| `model/TaczPolyMeshGunModel` | 代码全部一致（唯一一处是缩进空白） |
| `TaczMeshyIntegration` | Fabric `ResourceManagerHelper` ↔ NeoForge `AddClientReloadListenersEvent`（已在移植注释里写明） |
| `config/MeshyConfig` | `ForgeConfigSpec` ↔ `ModConfigSpec`；**净差 1 行**：她的 `MeshWorldMaxVertices` 注释有第三行 `Within MeshWorldFullDetailDistance the budget is waived entirely.`，我们漏了 |
| `core/PolyMeshSnapshot` | 行为一致（两边都没有那个镜内早退），**她多一段「为什么撤回」的说明** |
| `config/PolyRenderPolicy` | 全角破折号旁的空格，纯排版 |

配置键逐个数过：**11 个键两边齐**（含 `MeshGpuBaking` / `MeshGpuUnderShaders`），无单边缺项。

### 2.2 scope / iris / cloth 包

- 字节一致 6 个：`EtchedReticleRenderer` `IReticleRenderer` `IlluminatedReticleRenderer` `ReticleKind` `ReticleRendererRegistry` `ScopeNodeSet`
- 只差 `@Environment` 5 个：`ScopeMaskTextureHandle` `ScopeMaskGeometry` `ScopeMaskTarget` `ScopePipTarget` `ScopePipTrace`
- 只差 NeoForge 管线注册事件 2 个：`ScopePipRenderer` `ScopeBodyRenderTypes`（我们多 `registerPipeline(s)(RegisterRenderPipelinesEvent)`）
- 只差加载器 API 3 个：`IrisCompat`（Fabric Loader/Version ↔ `ModList`）、`MenuIntegration`（ModMenu ↔ `IConfigScreenFactory`）、`OpenGunPackDirEntry`（`FabricLoader#getGameDir` ↔ `FMLPaths.GAMEDIR`）
- 本仓是超集 1 个：`RenderClothConfig`（我们这边多出一整个 mesh 分区）
- **实质分歧 1 个**：`ScopeMaskRenderer` —— 见 §5.1

### 2.3 管线 location 名字不同，不是漏同步

她：`pipeline/scope_final_ocular_ring`（`FINAL_OCULAR_RING_PIPELINE`）
本仓：`pipeline/scope_ring_final`（`RING_FINAL_PIPELINE`）

核对结论：**自洽，不是漏项**。

- 本仓资源里有 `src/main/resources/assets/tacz/shaders/core/scope_ring_final.fsh` 与之对应；
- 全仓 grep 无 `scope_final_ocular_ring` / `FINAL_OCULAR_RING` 残留；
- **两边都没有把环登记进 Iris 的 mode 表**（她的常量块也只有 body/flash/reticle/text 六条）——
  这是刻意的：环与光影下的最终文字都走「不交给 Iris」的那条路，否则会被塞回光影管线
  再被后置 pass 盖一次。

---

## 3. 本轮同步了什么

1. `PapiManager` 改用 `net.minecraft.locale.Language` 单参 `getOrDefault`（与她同名同源），
   javadoc 补类名陷阱说明。
2. `PolyMeshSnapshot#write` 补回「撤回镜内早退优化」的理由（她侧内容，本仓行为本就一致）。
3. `MeshyConfig` 补回 `MeshWorldMaxVertices` 的第三行 TOML 注释。

---

## 4. 不同步什么，以及为什么

| 她有我们没有 | 判断 |
|---|---|
| `scope/ScopePipResourceProbe.java` | **暂不搬**。诊断工具（每 600 帧脉冲统计 GPU 纹理字节 + 堆/GC），针对的是她的未结症状「开镜帧率随时间衰减、重进存档重置」。本仓没有这个症状的报告；本仓 `ScopePipTrace` 针对的是另一个症状（放大画面溢出到镜外 → 追 target 解析）。为没复现的症状搬 ~300 行反射诊断，属于无的放矢。若哪天我们这边也出现「衰减型」fps 掉，再来搬。 |
| `renderer/feature/` 三件套 | **不搬**。Fabric FeatureRenderer API 实验；她自己的类注释写明 `GunModelSubmit`「全仓零调用、当前不会收到节点，不能描述成完整迁移」。本仓在役路径是 `FeatureRenderCompat` + `submitCustomGeometry`。 |
| `client/gui/GunPackProgressScreen.java` | **不搬**。26.2 早期移植产物（加载进度屏）。本仓端口里没有对应物，属于两仓各自移植时的取舍差异，不是修复也不是缺功能（本仓无此屏的 symptom 报告）。 |
| `compat/kubejs/*` `compat/ar/*` `compat/optifine` `compat/playeranimator/*` `init/ModPainting` `network/HandshakeNetworking` | **不搬**。她那份端口自带的兼容面 / 加载器握手，不在本仓这条同步血缘上。 |
| `cn/sh1rocu/tacz/api/event/*`、`util/forge/*`、`util/itemhandler/*`、`compat/modmenu`、`compat/rei/*` | **不搬**。Fabric 侧的 Forge API 垫片，本仓 NeoForge 直接用原生 API。 |
| `compat/iris/legacy/` + `newly/` | **不搬**。AGENTS.md 明令：`IrisScopePipelineCompat` / `VoxyScopePipelineCompat` 以及她那份 Fabric Iris 版本分流，不得整文件搬。 |

---

## 5. 反向：本仓领先的两处（要回单给她）

已写成 `docs/handoff/HANDOFF_TO_SISTER_26_2_MASK_HULL_AND_TEXT_20260831.md`。

### 5.1 掩码凸包的硬件裁剪 —— 她在用，我们已判定做不到并撤回

- 她的 `ScopeMaskRenderer` 最后一次改动是 **2026-08-22**（`3b42fc9`），仍是旧形态：
  `NDC_SANITY_LIMIT = 2.0f` + `maskBoundsNdc()` / `hasMaskBounds()`（硬件裁剪用的边界出口）。
- 本仓两次改动：
  - `9037125`（2026-08-29）**移除**镜内画中画的硬件剪裁 —— 手持投影在 26.2 上 **CPU 侧拿不到**；
  - `b5ac13d`（2026-08-30）MARK5 开镜渐开超过目镜 —— 掩码凸包的离群阈值量级换算错误
    （改成 `SLOPE_SANITY_LIMIT=3.0` / `RADIUS_OUTLIER_FACTOR=5.0` / `DEPTH_OUTLIER_FRACTION=0.25`
    + 中位数裁剪 `applyOutlierTrim`）。
- 本仓 grep 确认：`applyLensScissor` / `maskBoundsNdc` / `hasMaskBounds` / `setHandProjection` **零残留**。

⇒ 这一处**不能反向同步**（会把 26.2 上做不到的硬件裁剪搬回来，还会丢掉离群阈值那笔修复）。

### 5.2 光影下镜内文字是黑块 —— 她的 `c4eb4e2` 只修了一半

见 §1.1。mode 2 补的是「裁不裁」，黑块是「着色器谁接管」的问题，两者不是一个层。
本仓的解法是最终覆盖路线（`FINAL_TEXT_PIPELINE` + `ScopeFinalRingOverlay`）。

诚实状态：源码完成 + CI 编译过；**实机未在本轮验收范围内**，记录里查不到它的 PASS 依据。

### 5.3 mesh 第 1 步（GPU 静态烘焙）—— 在她 PoC 基础上实机 PASS

她的路线图第 1 步源自她的四个提交（`8191f6b` / `0ea0fb6` / `6e275d0` / `9f7412e`），
本仓搬的是这四笔的**最终形态**（`f70867d→9f7412e` 聚合差）。维护者 2026-08-31 实机
验收通过（`docs/MESH_LOADER.md` §5.2 第 8–12 条，含朝向、换弹、光影翻转、降级通道）。
这是**回单**，她那边可以据此把她路线图第 1 步标成已验证。

---

## 6. 状态

| 项 | 状态 |
|---|---|
| 她 → 本仓 | **已清空**：2 笔真实提交，1 笔本仓早有且更进一步，1 笔本轮改齐 |
| 文件级净差 | 3 处，本轮全部补平（提交 `833d8ac`） |
| 本仓 → 她 | 2 处领先已写成 handoff；mesh 第 1 步 PASS 已回单 |
| 她的 `HANDOFF_LEDGER.md` | 自 `f01864e` 以来 `docs/lineage/` **零改动**，仍是 08-30 那版：第 9 项（她→我们）本轮已清空，第 4 项（我们→她，scope-mask-order-independence）在她那边仍是「需核对」 |

CI：本轮改动 `833d8ac` → **BUILD SUCCESSFUL**（ci-log(4343dec)）。
顺带把「26.2 的查表类是 `net.minecraft.locale.Language`」这条从她的 Fabric 侧
**证到本仓 NeoForge 侧**：两边同一个类名都能编译。

---

## 7. 给下一轮的清单

1. 下次同步**先比树再读提交**——本轮的经验是：提交账本看不到「她有我没有」（`ScopePipResourceProbe`
   这类单边文件只有比树才会浮出来）。
2. `ScopeMaskRenderer` 是本仓唯一的实质分歧，且方向是**我们领先**；她若哪天动这个文件，
   得先读我们的 `9037125` + `b5ac13d`，否则会把硬件裁剪搬回来。
3. 若本仓出现「开镜帧率随时间衰减、重进存档重置」，直接去搬她的 `ScopePipResourceProbe`
   （GPU 字节 + 堆/GC 两格是现有 `ScopePipTrace` 没覆盖的）。
4. 光影下镜内文字（黑块那条最终覆盖路线）实机状态待确认——本轮记录里找不到它的验收依据。
