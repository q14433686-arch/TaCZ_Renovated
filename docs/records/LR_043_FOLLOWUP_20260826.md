# Official 0.4.3 follow-up (2026-08-26)

Source: Curse `8652673` / GitHub tag `0.4.3` (`de97def`). No ARR art. No PASS claim.

## Followed this round

| Item | Official 0.4.3 | This port |
| --- | --- | --- |
| Smoke light | Ambient + 6-neighbor fallback, floor 2 | Same packing via `Level#getBrightness`; no longer `0xF000F0` |
| Cook explode | `prepareTime + lifeTime`, then `onThrow` then `stopUsingItem` | Same threshold; 26.2 still `stopUsingItem` then `onThrow` with captured ticks |
| `display_offset` | Vector3f on melee / throwable / consumable | Parsed and applied after transforms, before model origin |
| `entity_transform` | `ItemTransform`, default Z90 + `(-0.3, 0.15, 0)` | Same default; JSON uses 1.20.1 pixel×1/16 parse; applied with 1.20.1 pose semantics (26.2 `ItemTransform#apply` would recenter -0.5) |
| Tooltip custom desc | Index `tooltip` key, gray, wrap 300, collapse >3 | Already present in `AbstractClientItemTooltip` |
| Consumable 1P | `ConsumableItemRenderer` + display channel | Added; no-content-pack still uses vanilla placeholder |

## Investigate-only — leave for next round

### TACZ 3P muzzle lock vs existing PAL — **no-go this round**

Official 0.4.3 changelog: TACZ third-person hold locked the muzzle. Official LR registers a global player_animator **rotation** layer (`AdjustmentYRotModifier`) on every player and always pitches `leftArm`/`rightArm`. TACZ PAL here already skips arm pitch when `GunDisplayInstance#is3rdFixedHand`. This port has **no** LR rotation layer, so the official bug cannot fire. Next round should only add the exemption if/when an LR rotation layer is introduced.

### Melee 3P player_animator — **no-go this round**

Official stack: `PlayerAnimatorIntegration` (upper/lower/rotation), `MeleeAnimationListener`, `IdleAnimationHandler`, `ThirdPersonAnimationConfig` on display JSON, mixin into TACZ `PlayerAnimatorAssetManager`, `PlayerModelMixin` 1P arm reset. Needs content-pack player_animator files, can fight TACZ PAL layers, and is not required for the 1P Bedrock/Lua path already in this port.

## Still abandoned

flash_shield, ARR art/models/sounds, standalone lrtactical mod.
