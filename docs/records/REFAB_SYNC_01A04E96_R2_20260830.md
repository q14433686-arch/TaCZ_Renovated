# 第二轮同步姊妹 `arena/01a04e96`（2026-08-30）

日期：2026-08-30
作者：Arena agent（本仓 `arena/01a04ea3-tacz-renovated`，26.2 / NeoForge）
依据：姊妹 `TaCZ_Refabricated_Unofficial` 分支 `arena/01a04e96` 上的
`docs/lineage/SYNC_GUIDE_RENOV_262_20260830.md`（其 HANDOFF_LEDGER 第 9 项，OPEN）
上一同步点：`e6c7f4c`（见 `REFAB_SYNC_01A04E96_20260830.md`）
本次取货点：`9f7412e`（她的分支尖端；其后的 `f273fe5` / `3351f99` 只是 lineage 文档）

> **纪律**：只搬语义，不抄 Fabric 表皮；先取证再动手；未实机一律不写 PASS。

---

## 0. 一句话结论

她的清单里 **7 项，本轮核完是「1 项有 bug 要修、1 项缺旋钮、1 项本来就齐、4 项按本仓既有裁决不同步」**。
最贵的那条（PIP 终态对表）查下来**不是落后，是刻意不同步** —— 详见 §2.3。

---

## 1. 她的七项 × 本仓现状（逐项核过）

| # | 她列的项 | 本仓核查结果 | 处置 |
|---|---|---|---|
| 1 | PR#82 泄漏修复（帧率衰减） | **已在**，逐行一致（§2.2） | 无需动作 |
| 2 | `9df8718` RERENDER_INTERVAL + ResolutionScale 作用域 | **缺 INTERVAL**，作用域注释也不全 | **已同步**（§3.2） |
| 3 | `4ec0dde` ShadowScale 热应用 | 依赖本仓**刻意没同步**的管线预热/释放装置 | **不同步**（§4.1） |
| 4 | `4aa8d7b`+`12d6f3c` 检视动画两连修 | **bug 同在**（基线逐字相同） | **已同步**（§3.1） |
| 5 | `94179d4` 裁手 + `9d03659` 裁字 | 本仓无；需先找 NeoForge 侧挂点 | 暂缓（§4.2） |
| 6 | meshloader 全套 | 本仓无；上一轮已裁定暂不移植 | 维持（§4.3） |
| 7 | `f70867d`+`9f7412e` mesh LOD/烘焙世代 | 随 #6 | 维持 |

---

## 2. 取证（本轮最重要的一段）

### 2.1 动画三件套基线逐字相同

`ObjectAnimationRunner` / `AnimationStateContext` / `AnimationStateMachine`
三个文件，本仓改动前与她侧 `4aa8d7b^` **逐字相同**（`diff` 比对，非目测）。
→ 她说的「bug 必然同在」成立，且补丁可以干净落地（`git apply` 零冲突）。

### 2.2 PR#82 泄漏修复：本仓**已经是修好的**

她的 §1 把它列为「优先核对，防潜伏」。实际比对 `shouldPreserveSubmits()`：

```java
// 两边完全一致（只差 LevelRendererAccessor 是否 import）
return current == ((…) mc.levelRenderer).tacz$getSubmitNodeStorage();
```

语义都是「只保留主画面的 `LevelRenderer.submitNodeStorage`，Iris 阴影那份专用存储
绝不保留」。本仓 CHANGELOG「新增：镜内画中画」段里也早记着这条（`052e600`）。
→ **无泄漏 bug，不需要动作。**

### 2.3 PIP 终态对表：差的 311 行全是「我们故意不要的」

她的文件 `1437` 行（9df8718^），本仓 `1126` 行 —— 看着像落后一大截。
去掉注释/空行/import 后只比**代码行**：

```
她们 615 行 · 本仓 584 行 · 差 31 行
纯代码差异：本仓缺 43 行 / 本仓多 12 行
```

**方法集合完全相同**（没有「她有我们没有」的方法）。缺的那 43 行逐条对上了本仓既有的
三条裁决，一条都不该补：

| 缺的代码 | 为什么缺 |
|---|---|
| `releaseIdlePipeline()` / `SCOPE_PIP_RELEASE_IDLE_PIPELINE` / `SCOPE_PIP_IDLE_RELEASE_DELAY_FRAMES` / `idleReleaseFrames` | 空闲释放管线是「光影下开镜帧率衰减」那次**未结案调查的探针**，本仓 CHANGELOG 明写「按裁决不同步」 |
| `ScopePipResourceProbe.onScopePassBegin/End` | 同上，诊断装置，不同步 |
| `applyLensScissor(...)` + `hasMaskBounds()` / `maskBoundsNdc()` | 实机两轮后**已移除**：26.2 上 CPU 侧拿不到手持那一遍的投影（详见 `REFAB_SCOPE_PIP_SYNC_20260830.md` §5.5） |
| `@Environment(EnvType.CLIENT)` | Fabric 注解 |
| `LevelRendererAccessor` 用 FQN 而非 import | 纯排版 |

本仓多出的 12 行：`registerPipeline(RegisterRenderPipelinesEvent)`（NeoForge 的管线注册表面）
与 `ScopeFinalRingOverlay.beginFrame()`（本仓自己的遮光环延后重画）。

→ **PIP 功能面没有落后**，行数差是「注释写得少 + 三处刻意不同步」。

---

## 3. 本轮同步了什么

### 3.1 检视动画两连修（她的最高优先）

两笔**必须一起拿**：

- `4aa8d7b`：让 `stopAnimation` 连坐 `transitionTo` —— 带过渡时长启动的动画在过渡完成前
  挂在旧 runner 的 `transitionTo` 上，只停当前 runner 等于停了个寂寞；
- `12d6f3c`：引入 runner **出生序号** + `AnimationStateMachine#trigger` 的转移前快照
  （`triggerSpawnFloor`，栈式恢复以支持脚本重入 trigger），豁免「本次 trigger 刚启动的
  后继动画」—— 只拿第一笔会引入「检视中换弹，换弹动画被 exit 的 stop 当场误杀」的新 bug。

病根她写得很清楚：开镜时检视，脚本 `inspect.transition` 的打断分支会被每 tick 的移动类
输入在 ≤50ms 内触发，必然落在 0.2 秒过渡窗口内 → 检视动画成了无主僵尸，状态机已回
idle，挂在 inspect 态上的打断手段全部失联，只有切枪/丢枪能救。

落地：三文件改后与她 `12d6f3c` 终态**逐字相同**，无 Fabric 残留，无加载器差异。
CI 已过（`c65d9dd`：BUILD SUCCESSFUL）。

### 3.2 `ScopePipRerenderInterval` + ResolutionScale 作用域

- 新旋钮 `ScopePipRerenderInterval`（1~4，默认 1）：二次渲染模式下镜内那一遍世界每 N 帧
  才真跑一次，其余帧复用离屏纹理里的上一帧成品。这是**光影下唯一砍得到大头的杠杆**
  —— 帧率对半的根因是整条 Iris 管线每帧跑两遍，降离屏分辨率救不了它。
- 两道守卫：`ScopePipTarget.generation()` 必须一致（target 重建后新纹理内容未定义）、
  帧差 `< N`（`frameIndex` 只在 `GameRenderer#extract` HEAD 递增，每帧恰一次，
  不受 Iris 一帧两趟手部 pass 影响）。首帧哨兵 `Long.MIN_VALUE` 会让
  `frameIndex - MIN_VALUE` **上溢成负数**，所以额外加了 `lastScopePassFrame >= 0`。
- `ScopePipResolutionScale` 的真实作用域（只在「二次渲染 + 无光影」生效）补进配置注释、
  Cloth tooltip、中英文文案，并在两条不消费它的路径各打一条一次性日志。

取证：`ScopePipTarget` 改动前与她 `9df8718^` 除 `@Environment` 与注释措辞外逐字相同；
四个插入点结构一致。按本仓 `ModConfigSpec.IntValue` 表面接入，未照抄 `ForgeConfigSpec`。

---

## 4. 不同步什么，以及为什么

### 4.1 `4ec0dde` ShadowScale 热应用 —— 依赖本仓刻意没有的装置

她的做法是：记下建管线时的 `appliedShadowScale`，在 `prewarmIfNeeded()` 里每帧比对，
变了就 `releaseScopePipelineIfPresent()` 销毁重建。
而本仓 `IrisScopePipelineCompat` 是 **219 行的精简版**（她的 353 行），文件头的移植说明
白纸黑字写着「未同步那套『空闲释放瞄具管线』的实验入口，它是未结案调查的探针，不是修复」。
热应用**就长在那套装置上**，拆不出来。

本仓现状是诚实的：配置注释末行写着 `Takes effect when the scope pipeline is built, so
restart or change dimension.` —— 限制已知、已写明，不是静默失效。
她的 `4ec0dde` 里另一半「钩子静默失效告警」（`shadowHookRanDuringBuild`）倒是**值得单独拿**：
`IrisShadowResolutionMixin` 是 `require = 0` 的软注入，Iris 改内部类名它就悄悄不生效，
把静默变告警对排查有价值。**留给下一轮。**

### 4.2 镜内裁手 / 裁字 —— 新特性，得先找本仓的挂点

两条机制她写得很具体：裁手是「在 collector 上做动态代理，把 `entityTranslucent(skin)`
的 buffer 请求换成带掩码裁剪的 `armClipped`」；裁字是「拦 `prepareText`，经 `visit` 后门
把字形 quad 徒手提交进 `scope_text` 裁剪管线，失败回退 vanilla `submitText`」。

可以搬，但两处要自己找 NeoForge 侧的等价物：她挂在 Fabric mixin 的 collector 入口，
本仓要找自己的手部渲染 collector 交接点。**而且这两条在她那边也还是「待实测」**
（她的治理提案 PR D 一栏：遮光环 PASS，裁手/裁字待实测）。本仓不做第一个吃螃蟹的，
等她实测过再搬，成本更低。

### 4.3 meshloader —— 维持上一轮裁定

`REFAB_SYNC_01A04E96_20260830.md` §3 已裁定暂不移植。另有一个悬而未决的前置问题：
**本仓用户到底用不用 `model_type=mesh` 的枪包？** 不用就是纯成本。这个问题还没答。

---

## 5. 状态

- 动画两连修：**CI 编译通过**（`c65d9dd`：BUILD SUCCESSFUL），**未实机**。
- RerenderInterval + ResolutionScale 作用域：**CI 编译通过**（`9330697`：BUILD
  SUCCESSFUL），**未实机**。
- 全部改动均为源码级同步，未实机验证，**不写 PASS**。

## 6. 给下一轮的清单

1. 等这两笔的 CI 编译结果，再请维护者实机验一轮（动画：开镜检视可被开火/换弹打断，
   且「检视中途开镜」不硬切；PIP：`ScopePipRerenderInterval=2` + 光影下的帧率与滞后）。
2. `4ec0dde` 的「阴影钩子静默失效告警」可以单独摘过来（不依赖预热/释放装置）。
3. 裁手/裁字等她实测 PASS 后再动。
4. meshloader 等「用不用 mesh 枪包」这个问题有答案。
5. 她的 ledger 第 9 项可以从 OPEN 改成部分 DONE（#1 无需动作、#2 #4 已同步），
   #3/#5/#6 仍是 OPEN。她建议本仓放一份 ledger 镜像 —— 要不要建，请维护者裁。
