# 26.2 R1 用户验收记录 —— L0-L3 PASS

日期：2026-08-21
用户回执：**“PASS。L0-L3 完美收官”**

## 环境与候选

- Minecraft 26.2
- NeoForge 26.2.0.64 release
- Java 25 / Gradle 9.2.1
- TaCZ `1.1.8+neoforge.26.2.R1`
- 测试指引对应分支 HEAD：`89e656aaf5c2745a7c6dfaebe2f5ee4e8c3dde15`
- 测试预案：`docs/DEDICATED_SERVER_TEST.md`

> 命名说明：测试执行时 artifact 仍使用开发标签 `r0`；用户随后将**同一代码内容**定名为
> 26.2 R1，本记录的版本展示已按最终名称统一。逻辑层 L1-L3 结论不变，但 R1 metadata
> 变化后的最终 jar 文件名、mods.toml 与 Mod List 仍需补跑一次 L0/L1 快速确认。
>
> 本记录依据用户直接回执冻结。该消息没有附带新日志或逐项数字，因此不自行补写枪包
> 计数、启动耗时或客户端名称；只记录用户明确给出的层级结论。

## 结果

| 层级 | 结果 | 覆盖面 |
|---|---|---|
| L0 构建后静态自检 | ✅ 用户 PASS | 当前 jar、jar-in-jar、mixin/AT、metadata/version 展开 |
| L1 `runServer` | ✅ 用户 PASS | 开发环境 server dist、资源/网络注册、`Done`/退出 |
| L2 生产 jar + 真实 NeoForge 专服 | ✅ 用户 PASS | 生产类加载、jarJar、AT/mixin、默认包导出、重启 |
| L3 双客户端联机矩阵 | ✅ 用户 PASS | 用户确认该层整体通过；本回执未附逐行日志，因此不单独外推 PAL、F3+B 或高延迟可选行 |

L3 的 PASS 同时回归了本轮从 26.1.2 R1 回流的关键路径：

- `ServerMessageGunDraw` EMPTY previous/current 使用 optional stream codec；
- RECIPE_FILTER / ATTACHMENT_TAGS 进入 common network cache；
- 四类物品名称走 common index，dedicated `/give` 不加载 client classes；
- Iris already-assigned 分类不再误判失败。

## 不外推的项目

- L2.5 第三方枪包装载专项在本条回执中没有被单独点名，继续保持“待明确确认”；
- OpenGL/Iris/Vulkan 的完整 scope-mask GPU 矩阵不由 L0-L3 自动覆盖；
- 可选 Mod 逐项矩阵仍以 `COMPATIBILITY.md` 为准；
- 最终 source archive、tag、发布日期与发布上传仍未完成。

## 结论

定名前同代码候选的生产编译、开发专服、真实专服与基础双客户端多人逻辑闸门关闭。
最终 R1 metadata 产物仍需 L0/L1 快速确认；发布另受 L2.5、GPU/可选 Mod 矩阵及最终归档
一致性检查阻塞。

## 2026-08-22 后续范围说明

本回执之后，当前 R1 又前滚了 26.1.2 R1 的 LRTactical 内置层。该增量改变 Java、资源、
AT、mixin 与 payload，不属于“只改 metadata”。因此本记录继续证明 LR 合入前核心候选，
但不作为当前 LR-integrated HEAD 的 L0-L3 或 LR PASS；新门禁见
`../records/RELEASE_CHECKLIST_26_2_R1.md` 与 `LR_R1_SYNC_26_2_20260822.md`。
