# 26.1.2 → 1.21.11 同步文本 · 渲染线（2026-09-01）

> 【26.1.2 → 1.21.11 同步 · 渲染线 2026-09-01】
> 范围：本分支 `2179e36`（上一份同步文本之后）至 `5a2f280` 的全部渲染改动。
> 26.1.2 侧状态：**全部编译级验证（CI 绿）+ 实机 PASS（用户亲测，含今日全部反馈轮）**。
> 1.21.11 侧状态：**零验证**。下述「已有/缺失」判断基于本会话对 1.21.11 分支树
> （refs/coord-1211 @ `540baf9`）的**文件级/函数级抽查**（git 读取，未编译未运行）。
> 人工核对清单见 §6——如对某项没把握，按 §6 的 commit 清单逐条核对真实提交即可。

## 0. 1.21.11 侧已有设施（抽查确认，无需重做）

- `ScopeDepthCopyState`：完整掩码周期（BACKUP/APERTURE_COPY/MASK/MASK_OUTSIDE、
  `prepareMaskDraw`、`isMaskCycleValid`）——掩码线本来就出生在 1.21.11。
- `ScopeRenderTypes` + `scope_flash_clip.fsh` + `IrisDepthRestoreShaderMixin`
  （ShaderCreator 打补丁注入 hand 着色器）。
- `PolyMeshGpuRenderer`：LIT/EMISSIVE 自建管线、`drawList`、
  `renderAtHandFlush`/`renderAtWorldFlush` 双消费点、`isInsideScopeLevelRender` 门。
- `ScopePipRenderState`：`compositeAfterHand`（**已含** rerender→`hasScene()` 闸）、
  `allowShaderPacks`/`needsIrisWorldDepthCopy`/`supportsFinalScopeOverlay`。
- `ScopePipRerender` 基础体（窄遍 + 隔帧复用）+ `GameRendererMixin` 的 renderLevel redirect。
- `ItemInHandRendererMixin` 对 `renderHandsWithItems` 的 RETURN 注入点
  （1.21.11 的手部 flush 在 `renderHandsWithItems` 自己的批次里，注入点语义等价）。

## 1. 建议第一批：mesh 目镜裁剪线（本次主线，独立性最好）

| 26.1.2 commit | 内容 | 1.21.11 移植要点 |
|---|---|---|
| `ee77059`+`e203984` | `mesh_entity_scope_clip.fsh`（scope_flash_clip 的 mode-2 硬编码克隆）+ `LIT_PIPELINE_CLIP`（带两深度采样器的克隆管线）+ drawList 无光影路线（RenderPass 采样器绑定）+ `ScopePipRenderState.worldDepthViewFor/apertureDepthViewFor` 公有访问器 | 资源文件直接拷；管线克隆按 1.21.11 的 `LIT_PIPELINE` 定义（`PolyMeshGpuRenderer.java:145`）同款 builder；访问器连同样式的 `ImportedDepthTexture(View)` 懒建一起带 |
| `d6743e5` | 光影路线：`ScopeDepthCopyState.beginExternalMaskOutsideDraw()`（=begin(MASK_OUTSIDE)+beforeDraw()，GL-uniform 翻转打补丁的 hand 着色器）+ drawList `irisFlush` 分流 + try/finally `end()` | 1.21.11 **已有** `prepareMaskDraw`/`MASK_OUTSIDE`，此入口是 vanilla RenderType 序列的原样暴露，直接搬；注入分支（IrisDepthRestoreShaderMixin）1.21.11 已有，需核对其 Iris 版本的 ShaderCreator 签名仍匹配 |
| `752ee9e`+`3a62d00` | 帧戳时效闸：`onClientFrameStart()`（GameRenderer.render HEAD，帧计数 +1）+ `maskCycleFrame` 盖戳 + `hasMaskCycleThisFrame()`；**取代**裸 `isMaskCycleValid()` 做闸 | 1.21.11 现有消费者（scope text 等）仍用 `isMaskCycleValid` 的语义不变；新增帧戳字段与两个入口即可。没有帧戳闸，腰射态会出现「目镜形状的透视面残留在枪身初始/退出位置」（实机已证） |
| `5a41777` | `renderAtHandFlush` 在 `isInsideScopeLevelRender()` 时清表早退（手部坐过 PIP 窄遍） | 直接搬。1.21.11 光影下手部同样在 Level 遍内（HandRenderer），窄遍同样包含手部阶段；不清则高模枪在镜内留「枪前端残影」（实机已证） |

依赖关系：`d6743e5`/`752ee9e` 依赖 `ee77059` 的管线与访问器；建议整批一个 PR。

## 2. 建议第二批：PIP 合成修正（贴屏三病根治）

| 26.1.2 commit | 内容 | 1.21.11 移植要点 |
|---|---|---|
| `f3e0f9c` | `compositeAfterIrisFinal` 补 `rerenderMode() && !hasScene()` 早退 | 1.21.11 的 `compositeAfterHand` 已有同款闸（:523）；**核对**其 `compositeAfterIrisFinal`（:542 起）是否缺失，缺则补。缺的后果：退镜后静态「截图」贴屏 |
| `2f0e81c`+`6f7b690` | 两个合成入口加 `hasMaskCycleThisFrame()` 时效闸（fail-closed） | 依赖 §1 的帧戳设施。注意终局钩子时序结论：**Iris 26.1/1.11.x 把手部搬进 Level 遍内，finalizeLevelRendering 在同一遍手部之后**——合成用当帧拷贝即可，勿用「上一帧」语义（该错误实现曾致光影下二次渲染整体失效） |
| `7eca413`+`5a2f280`+后续「重投影同阈」提交 | `PIP_REVEAL_THRESHOLD = 0.35`：**两种模式同一条**显示阈（引擎层接管=开镜瞬间；捕获闸、`needsIrisWorldDepthCopy` 闸、两路合成闸四处联动，捕获/拷贝必须早于首帧显示就绪）。`IRIS_FULL_AIM_THRESHOLD`（0.995）已删除——用户最终裁定：重投影也做开镜即接管，过渡期镜内无画面像 bug | 直接搬。**历史教训（务必读）**：中途的 `b9f9db7` 曾把全 ADS 门扩到两种模式（把「一帧陈旧截图」与「过渡期正常镜内画面」混淆），用户实机否决——**勿移植 `b9f9db7`**。滑入中段的放大镜环残影（实机已复现）由倍率渐变处理：显示阈之上重投影倍率从 1（恒等贴回=镜孔区域的干净 1x 画面）按进度线性步进（10 档、管线按档缓存有界构建）到稳态——26.2 母版实机观感的来源即此渐变，26.1.2 以量化管线替代其未验证的逐帧 ColorModulator uniform |

## 3. 第三批（大件，可独立排期）：光影下二次渲染整线

1.21.11 **缺失全部隔离设施**（抽查 MISSING）：`IrisScopePipelineCompat`、
`VoxyScopePipelineCompat`、`IrisScopeDimensionMixin`、`ScopePipReleaseIdlePipeline`、
`LevelRendererAllChangedScopePassMixin`。涉及 `825d2c5`（放行光影）、`3e8b22e`+`1c2c5b5`
（管线隔离 + 预热 + 配置）、`95590b0`（窄遍反馈回路斩断）、`82b3262`（ShadowScale）、
`c42b047`（idle release）、`2027261`+`7b4a9a2`（Voxy 第二渲染栈 + reload 钩子）、
`3d8432f`（Sodium 私有投影快照同步）、`d3f0fdc` 的 ESC 三道闸半部。

1.21.11 侧差异与简化：
- **reload 钩子方向相反**：1.21.11 有 `LevelExtractor.allChanged`（26.1.2 没有），
  `7b4a9a2` 的 `LevelRenderer.allChanged` 适配在 1.21.11 应改回原生 `LevelExtractor` 路线。
- 其余（隔离dimension、预热窗 allChanged 压制、Voxy 坐过/换栈、idle release 熔断）
  逻辑同构，按 26.1.2 版文件逐个搬即可。
- **注意**：1.21.11 的 `renderScopeView` 在光影下的当前状态需先核对（§6-③）——若已
  放行但无隔离，会复现 26.1.2 修过的三症状（整屏拖影/云噪点闪烁/镜外发糙），建议本批
  与第二批一起上或先明确拒绝。

## 4. 第四批：周边修正（顺手）

| 26.1.2 commit | 内容 | 1.21.11 要点 |
|---|---|---|
| `03a807e` | scope text + tooltips 纯查找表替代 `I18n.get`（Format error） | 直接搬；1.21.11 同款风险 |
| `58831e4`+`0651171` | Cloth/FCAP 显式持久化（FCAP save() no-op 规避） | **先核对 1.21.11 的 FCAP 版本**是否同样 no-op，再决定搬或跳过 |
| `2ae4c29` | 纹理解析移到 render pass 外（pass 内 writeToTexture 被拒） | 1.21.11 的 drawList 注释本就引用该限制（疑似已内置），核对后大概率跳过 |
| `93178de` | 法线 MV 栈修复 | 已有独立同步文本（`MESH_GPU_NORMAL_MATRIX_2612_20260901.md` §4），按那份走 |
| `99e505f` | 世界消费点拓扑（26.1.2 特有） | **不移植**：1.21.11 的消费点拓扑本就不同且已适配（`renderAtWorldFlush` 原生分派） |

## 5. 不移植 / 已作废

- `b9f9db7`（全 ADS 门扩大到二次渲染）——被用户实机否决，`5a2f280` 恢复 0.35。
- TEMP ci probe 系列（`7eb7a2b`…`b9ec08c`、`9626fe7` 等）——一次性探针，已无价值。
- `ba4f720`/`3cf9d0f`（RawOutput.log 上传/删除）——崩溃排查附件，与移植无关。

## 6. 人工核对清单（我没把握、请对真实 commit/1.21.11 树复核的点）

1. **§1 Iris 路线前提**：1.21.11 的 `IrisDepthRestoreShaderMixin` 对其 Iris 版本的
   `ShaderCreator.link` ordinal=4 是否仍命中（26.1.2 侧 Iris 1.11.x 核实过，1.21.11
   的 Iris 版本可能不同）。不命中则 mesh 光影裁剪静默 fail-open（不裁剪，不炸）。
2. **§1 drawList 结构差**：1.21.11 的 `drawList` 与 26.1.2 版在 UBO/索引预热/纹理解析
   顺序上的差异（注释互引过 1.21.11 行为，大概率同构）——套 clip 分流时逐行对一遍。
3. **§3 前置**：1.21.11 `ScopePipRerender.renderScopeView` 对光影的当前闸状态
   （硬拒 vs 放行无隔离）。
4. **§2**：1.21.11 `compositeAfterIrisFinal`（:542 起）现有的门栈全貌。
5. **§4**：1.21.11 的 FCAP 版本（config 持久化是否同样需要显式 save）。

## 7. 验证状态声明

- 本表所有 26.1.2 commit：CI 编译绿 + 实机 PASS（用户，2026-09-01）。
- 1.21.11 侧：仅文件级/函数级存在性抽查（refs/coord-1211 @ `540baf9`），
  **未编译、未运行**。全部标注未验证。
