# 26.1.2 补课包：把「最终覆盖」从 1.21.11 搬过来

日期：2026-08-30 · 生成自本仓 `origin/1.21.11`（tip `e3d9dd5`）
用途：给**开在 `26.1.2` 上的新会话**用。本会话被钉在 26.2 分支，不能动 `26.1.2`。

## 目录

| 文件 | 是什么 |
|---|---|
| [`PORT_CONTRACT.md`](PORT_CONTRACT.md) | **先读这个**。逐文件/逐 API 的移植契约 + 四个版本差异核对点的**已查证答案** |
| [`final_overlay_62532f1.diff`](final_overlay_62532f1.diff) | 1.21.11 上引入该功能的**原始改动集**（15 个文件，1678 行），权威蓝本 |
| [`../HANDOFF_26_1_2_CATCHUP_20260830.md`](../HANDOFF_26_1_2_CATCHUP_20260830.md) | 上层说明：为什么搬、症状、验收清单、可直接粘贴的提示词 |

## 怎么用它

1. 读 `PORT_CONTRACT.md` 的 §1（逐文件契约）和 §2（API 核对表）。
2. 对着 `final_overlay_62532f1.diff` 搬。**不要 `git apply`** —— 两个分支的
   `BedrockAttachmentModel` / `ScopeRenderTypes` 上下文不同，它是**蓝本不是补丁**。
3. 每搬完一个文件就按 §3 的自检表过一遍。

## 关键事实（查证过，不是推断）

- 该功能在 1.21.11 上是**一次提交引入的**：`62532f1`
  （`port(1.21.11): WP-11211 回移植 + …`，2026-08-22，102 文件）。
  本 diff 只取其中与本功能相关的 **15 个路径**。
- **`62532f1` 之后 1.21.11 上没有任何后续修正**（`git log 62532f1..origin/1.21.11`
  对这 7 个关键路径全为空）—— 也就是说蓝本是稳定版，不存在「后面还打过补丁」的隐患。
- 26.1.2 上**案例⑨ 的地基已经在**：`BedrockAttachmentModel:137` 的 `ocularRingPart`、
  `:538` 的 `captureStandalonePart`、`:632` 的行内提交都在。缺的只是「延后重画」那一层。
- 26.1.2 的 `tacz.iris.mixins.json` 只有 1 项（`IrisDepthRestoreShaderMixin`），
  1.21.11 有 3 项 —— 两个新 mixin 都要登记进去。

## 状态

**全部未编译、未实机。** 这是源码级取证 + 蓝本，按仓库规矩标 UNVERIFIED，
不要写 PASS，等实机反馈再收口。
