# 26.1.2 ← 姊妹 01a05170：Scope PIP + TML Mesh GPU 层移植记录（2026-09-01，NeoForge 同步版）

> 任务来源：把 Fabric 姊妹仓（TaCZ_Refabricated_Unofficial）`arena/01a05170` 分支在
> `9a4e71fb` 之后的全部实质内容等价移植到本仓（NeoForge 26.1.2）。
> 范围 = `9a4e71fb..5590000` 的 34 个实质提交（另有 25 个 ci-log/TEMP/docs/工具提交不搬）。
> 上一任务（Iris 延后重画回搬 caeb9e27，PIP 前置地基）已随 `03fa9ed` 在库，不在本次范围。
> 按「搬语义不照抄」执行；姊妹已按 26.1.2 官方未混淆符号落地，本仓再按 NeoForge 加载器差异适配。
>
> **补丁 v4（2026-09-01 四次同步）**：重新 fetch 后确认 `01a05170` 头已推进到 `5c45a787`（ci-log
> for `106fac2a`），自 v3 同步点 `3d8432fa` 起新增 <b>24 个实质提交</b> + 若干 ci-log（另有 4 个
> build.gradle-only TEMP probe 已随姊妹删除、不搬）。主题：PIP 光影二次渲染（Iris 专属管线隔离
> `IrisScopeDimensionMixin`+`IrisScopePipelineCompat` 预热/`ScopePipShadowScale`/空闲释放、
> Voxy 第二渲染栈、`LevelRenderer.allChanged` reload 钩子）、mesh GPU 目镜裁剪（新 fsh
> `mesh_entity_scope_clip` + Iris hand 分支双路线）、掩码周期帧戳（`hasMaskCycleThisFrame`
> fail-closed）、重投影倍率渐变（`REVEAL_RAMP_STEPS=10` 管线 map 缓存）、mesh GPU 手部让位、
> 合成阈值演进至 `PIP_REVEAL_THRESHOLD=0.35`。移植方式：5 个纯 Scope 文件与姊妹终版
> **逐字节一致**（本地基线 = 姊妹 `3d8432fa` 逐字节 SAME，无偏移）；Voxy/Iris 新文件整文件
> 拷入 + NeoForge 适配（`FabricLoader→ModList`、mixin 插件 `ModList.get()` 未初始化 → 沿用
> `FMLLoader.getCurrent().getLoadingModList()` 现成模式、`@Environment→@OnlyIn`）；手工合并：
> `PolyMeshGpuRenderer`（保留 v2 收口的 nearestSampler/首帧日志条款）、`RenderConfig`/
> `RenderClothConfig`（ModConfigSpec/Cloth 本仓形态 + 4 新键）、`GameRendererMixin`（帧首
> prewarm + `onClientFrameStart`，按本仓 `ShaderStateTracker` 相位顺序插入）、mixin json×3 +
> `neoforge.mods.toml` 注册 `tacz.voxy.mixins.json`（fabric.mod.json +1 行的 NeoForge 等价物）。
> Voxy 反射桥前提同 Sodium 结论：Voxy 若为 Fabric-only 则本仓静默降级（镜内无 LOD 远景、
> 主画面不受影响）；Iris 光影下 mesh 裁剪/PIP 隔离均标注未实机。**注释修正**：姊妹终版
> `IrisScopePipelineCompat` 类头「相对 26.2 母版的裁剪」段落在后续提交落地
> （Voxy 第二套栈 / 阴影降采样 / 空闲释放）后未更新，属过时陈述 —— 本仓已就地改正
> （单文件局部化，其余注释逐条保留）。
>
> **补丁 v3（2026-09-01 三次同步）**：用户再次要求复查。重新 fetch 后确认 `01a05170`
> 头已推进到 `e18c298c`，自 `5917827`（上次补丁点）起新增 1 个实质提交 `3d8432fa`
> （scope pip: sync Sodium's private terrain projection snapshot for the rerender pass）
> + 4 个 TEMP ci probe（只动 build.gradle，已随姊妹删除，不搬）+ 其 ci-log。
> 已移植：`SodiumCompat`（NeoForge 适配：FabricLoader→ModList；纯反射、类名
> `net.caffeinemc.mods.sodium.*` 跨平台一致）、`ScopePipRerender`（三处投影通道 +
> finally 还原 + `resetChunkUniformUpload`；与姊妹终版逐字节一致）、根因文档。
> **通用性结论（用户问"应该是通用的，也不一定"）**：修复逻辑通用 —— Sodium 官方
> 0.6.0+ 同代码库发布 NeoForge 构建，modid/内部包名与 Fabric 一致，反射桥直接成立；
> "也不一定"的部分 = NeoForge 侧若装的是旧 fork Embeddium（`net.embeddedt.embeddium.*`
> + `embeddium` modid）或没装渲染优化，本桥按设计安静降级（镜内地形不跟随放大，
> 不崩不报错），已在 `SodiumCompat` 类 javadoc 写明边界。
>
> **补丁（2026-09-01 二次同步）**：用户指出同步点落后。重新 fetch 后确认 `01a05170`
> 在 `5590000` 之后新增<b>且仅新增</b> 1 个实质提交 `2ae4c29`
> （mesh gpu: resolve/bind texture views outside the render pass）+ 其 ci-log `5917827`。
> 已按「pass 体内不变量」整族移植：`2ae4c29`（纹理视图整批在 `createRenderPass` 前解析、
> 失败日志 per-texture log-once），并同收 1.21.11 线 `01a05759` 上同一不变量的演进收口
> `99e36e28` 的 lightmap nearest sampler 提至 pass 外、`26bb33c5` 首帧判据日志挪出 pass 体
> 与类 javadoc「pass 体内不变量」正式条款。用户口中的「PIP 修复」经取证对应姊妹
> `01a0518d`/`01a05759`（`gradle.properties: minecraft_version=1.21.11`，即 26.1.2→1.21.11
> 回迁线）上的 8 个 PIP 提交（FOV gate ×3、magenta 诊断、reti/rim 加固、Iris-run PIP path、
> config 面）——<b>这些修复在 `01a05170` 终版中已全部吸收</b>（CameraSetupEvent 两线逐字节
> SAME；ScopePip* 符号并集 01a05170 ⊇ 01a0518d；lang 键 01a0518d 独有 = 0），无需再搬。
>
> **补丁 v5（2026-09-02）**：同步姊妹新分支 `01a05db3`（头 `05bad697`，分叉点 = v4 同步点
> `5c45a787`，无分叉偏移）。范围 = 7 个代码实质提交（`f8f5ed9c`/`091dd5ec`/`323cb69d`/
> `033d30f1`/`f086f36d`/`db360639`）+ 1 个 docs 提交（`70ce00c6`，选择性搬）+ 7 个 ci-log
> （不搬）。主题与迁法：
> - **tacz:nbt 材料**（`f8f5ed9c`）：姊妹的 Fabric `CustomIngredient`（`TaczNbtIngredient`）
>   <b>不照抄</b>——本仓消费 NeoForge 原生 `ICustomIngredient`（`PartialNbtIngredient` 已有
>   `strict` 字段覆盖 partial/full 两语义，注册键 `neoforge:ingredient_type: tacz:partial_nbt`）。
>   只在 `RecipeCompat` 归一化：`SUPPORTED_FORGE_INGREDIENTS` 加 `tacz:nbt`、`items`
>   字符串→数组（Upgrader 形态）、无 type `{item+nbt}` 隐式改写为 partial_nbt（不再静默丢
>   nbt）+ 日志；`GunSmithTableIngredient` catch 加 `LinkageError`、错误日志带归一化原文。
> - **FCAP 配置落盘桥**（`f8f5ed9c`/`091dd5ec`）：<b>不适用</b>——本仓无 FCAP，
>   `GunMod` 用 `modContainer.registerConfig`，Cloth 保存走 `spec.save()`；只搬等价物
>   `/tacz overwrite` 显式 `PreLoadConfig.spec.save()`（姊妹调 `ConfigPersist.saveAll()`，
>   本仓无此方法）。
> - **开镜距离补偿**（`f8f5ed9c`）：`ScopePipRenderState.currentDetailZoom()`（公式
>   `1+(zoom-1)·progress`）+ `PolyRenderPolicy` 两道距离闸门 `×detailZoom()`（提交侧补偿）。
> - **worldZoomForcedToOne 加固**（`f8f5ed9c`）：加 `scopePassRunnable()` 判据
>   （`!isUsingRenderPack || shaderRerenderAllowed`），镜内那遍被拒时不再把世界压成 1×。
> - **烘焙额度与 LRU 容量解耦**（`091dd5ec`）：`MeshyConfig.GPU_BAKE_BUDGET_PER_FRAME`
>   （默认 4，1-64）+ `tryReserveBake()` 无参化 + 「额度耗尽」log-once + Cloth/lang（本仓
>   语言键按既有 parity 脚本约定用 `mesh_gpu_bake_budget_per_frame`，非姊妹的
>   `mesh_gpu_bake_budget`）。**镜内文字 log-once**（`091dd5ec`）同收。
> - **镜内裁手**（`323cb69d`）：`RenderHelper` 6 参加 `clipToScopeExterior` + 动态代理
>   `wrapForScopeClip`（identity 换 RenderType → `flashTranslucentClipped`，失败回真
>   collector）；`Left/RightHandRender` extract 期取
>   `ScopeRenderTypes.viewmodelFxClipApplies()` 传入。本仓 `AvatarRenderer` 6 参调用形态
>   （`slim+player`）与姊妹 5 参不同，保持本仓既有调用。
> - **低倍镜不裁**（`033d30f1`/`f086f36d`）：`viewmodelFxClipApplies()`/
>   `magnificationSupportsLensClip()`（`ScopePipMinMagnification`，默认 4×）——手/火光/
>   枪身/配件换成新闸门；mesh GPU apertureClip 加同一条倍率线；
>   `ScopePipRenderState.minMagnification()` private→public。
> - **PIP 二次渲染世界 mesh 不烘焙修复**（`db360639`）：`shouldSubmitGpuWorld`/`worldSubmitBlocker`
>   移除镜内拒收（26.1.2 每遍 `renderLevel` 重提交世界几何）；`renderAtWorldFlush` 镜内那遍
>   照常画、<b>同样清表</b>、不记 `worldConsumedFrame`（主遍再消费自己那份）+ log-once
>   `[TacZMeshLoader] GPU world mesh pass active inside the scope PIP...`。
> - **deps**：无新依赖；未动版本号（不触发 §1 三件套）。
>
> **v5 状态：源码级移植完成，未编译、未实机。** 本仓文档面：18 文件、+407/-54；本地
> `check_lang_keys`/`check_mesh_config_parity`（19 项齐平，含新键）/`check_mixin_registration`
> 三脚本通过；CI compile-check/consistency 按既有流程等用户侧自动触发后核验，不自证。
> 未验证项沿用 v4 清单 + 本轮 6 项（tacz:nbt 实机配方解析、开镜距离补偿实机、裁手实机、
> 低倍率闸门实机、PIP 二次渲染 mesh 烘焙修复实机、Iris 光影下整族）。

## 1. 结论与状态

本次移植两组功能 + 一组修复：

- **Scope PIP（镜内画中画）**：配置面（`RenderConfig` 11 键）→ 深度快照（`ScopeDepthCopyState`
  只读 DepthHandle）→ 裸缘延迟（`ScopeFinalOverlayState` 支持仅镜框延迟）→ 渲染态/二次渲染/
  深度调试（`ScopePipRenderState/ScopePipRerender/ScopePipDepthDebug` + 4 个 shader）→
  接线（FOV/手部/Iris final）→ 二次渲染后逐帧状态重提取（`LevelRendererAccessor`）→
  镜内 `text_show`（`ScopeTextSubmitter` + 孔径裁剪 + `TextShowRender` 三参构造）→
  `RerenderInterval`（隔帧重渲）→ `I18n.get` → `Language` 纯查表（text_show + 两处 tooltip）。
- **TML Mesh GPU 层**：内置 TML 移植（`com.tacz.guns.compat.meshloader` 20 文件）→ 26.1.2
  flush 点集成（手部 RETURN 钩子 + 世界 `renderSolidFeatures` RETURN 消费）→ GUI 提取窗口
  （`ScreenRenderTracker`）→ `FeatureRenderDispatcherMixin` 注册 → 延迟 scope 文字改走
  存储 → 两个光影键默认回 ON（R3 裁定）→ 世界实心 flush 消费 WORLD_DRAWS → 每 draw 推
  骨骼 pose 到 MV 栈（光影法线修正）。
- **动画**：`stopAnimation` 放过同触发后继 / 转移中 runner（出生序号判据）。
- **配置**：`MeshyConfig` 18 键（`mesh_loader` 段）+ Cloth 18 条 + 语言键；
  `RenderConfig` 新增 PIP 11 键（驼峰 TOML 名对齐 26.2）+ Cloth 11 条 + 语言键。
- **lang**：`en_us/zh_cn` 各并入姊妹桶 59 键（Mesh 18 键族 + PIP 11 键族 + 杂项），
  保留本仓既有 157 个独有键，现各 514 键。
- **注册**：`tacz.mesh.mixins.json`（4 个 meshloader mixin）+ `neoforge.mods.toml [[mixins]]`；
  `tacz.mixins.json` 注册 `FeatureRenderDispatcherMixin` 与 `LevelRendererAccessor`。

**状态：源码级移植完成，未编译、未实机。** 本沙箱无 JDK（apt 非 root、Maven/Adoptium 不可达），
`./gradlew build` 不可行；远程 CI（`94af139` compile-check workflow，用户侧已跑）由用户触发，
推分支后等结果。**不要写 PASS；等用户实机/CI 反馈再收口。**

## 2. 加载器差异映射（动手前取证）

| 姊妹（Fabric） | 本仓（NeoForge 26.1.2） | 说明 |
|---|---|---|
| `RenderTickEvent`(START/END, SimpleBedrockModel) | `GameRenderer#render` HEAD/RETURN 内联（`PolyMeshGpuRenderer.beginFrame()` + `ShaderStateTracker.onRenderFrame()`） | Fabric 事件只在 FML client 期分发；NeoForge 无该事件，帧首工作内联到同一相位 |
| `ScreenEvents.beforeExtract/afterExtract` | `ScreenEvent.Render.Pre/Post`（参数 `GuiGraphicsExtractor`） | 26.1.x 源码核实（GitHub API 读取 NeoForge 26.1.x `ScreenEvent.java`） |
| `ResourceManagerHelper.registerReloadListener` | `AddClientReloadListenersEvent.addListener(Identifier, PreparableReloadListener)` | 客户端构建期 IModBusEvent；本仓 `PlayerAnimatorCompat` 同款模式 |
| `FabricLoader.isModLoaded / getModContainer` | `ModList.get().isLoaded / getModContainerById` | 版本读取用 `getModInfo().getVersion()`（Maven `ArtifactVersion`，`GunPackLoader.stripBuildMetadata` 同套路） |
| `@Environment(EnvType.CLIENT)` | `@OnlyIn(Dist.CLIENT)` | sed 迁移 + 逐文件核对 |
| `ForgeConfigSpec` | `ModConfigSpec` | `MeshyConfig`/`RenderConfig`/`PolyMesh` 全部类型已替换 |
| Fabric `ViewportEvent.ComputeFov`（SimpleBedrockModel） | NeoForge `ViewportEvent.ComputeFov` | 方法名同形（usedConfiguredFov/getCamera/getPartialTick/getFOV/setFOV），仅 import 不同 |
| `BeforeRenderHandEvent.CALLBACK.invoker().post` | `NeoForge.EVENT_BUS.post` | ItemInHandRendererMixin |
| `IrisCompat.Newly/Legacy` 版本分界（FabricLoader `Version.compareTo`） | `DefaultArtifactVersion("1.7.0")` Maven 版本比较 | 两桥类本身加载器无关（纯反射），原样复制 |

## 3. Fabric-only 明确排除（姊妹有、本仓不搬）

1. `ConfigPersist` + `MenuIntegration` 的 `root.setSavingRunnable(ConfigPersist::saveAll)`：
   FCAP v26.1.5 `save()` 在新架构下是静默 no-op 的修复。NeoForge `ModConfigSpec.save()` 原生
   落盘，本仓 `MenuIntegration` 保持 `CommonConfig.spec.save(); ClientConfig.spec.save();`。
2. `ForgeConfigSpecAccessor`（FCAP `childConfig` 暴露 + TOML 显式写回路径）：同属 FCAP 修复，
   已删除（未注册，防 `net.minecraftforge.common.ForgeConfigSpec` 在 NeoForge 26.1.2 缺失导致
   编译失败）。
3. Fabric 入口侧改动（`TaCZFabric`/`TaCZFabricClient`/`fabric.mod.json`/`build.gradle`/
   `gradle.properties` R3 版本 bump/README）：加载器专属，不搬。
4. `SoundEngineMixin` 26.1.2 官方 lambda 名修复：姊妹明确「stays unregistered」，本仓同判不注册。

## 4. 手工合并文件（非逐字节，语义对齐）

| 文件 | 处理 |
|---|---|
| `GameRendererMixin` | `renderItemInHand` HEAD/RETURN：`setInHandPass(true/false)` + `captureScene` + `compositeAfterHand` + 非 Iris 时 `ScopeFinalOverlayState.renderAfterFinalComposite`（`hasPendingOverlay() || isEnabled()` 门）+ `ScopePipDepthDebug.renderAfterHand`；`render` HEAD：`beginFrame()` + `ShaderStateTracker.onRenderFrame()`；`renderLevel` `@Redirect`：`setLevelRenderActive` try/finally 包 `ScopePipRerender.renderScopeView(...)` + 原 `renderLevel` |
| `ItemInHandRendererMixin` | `@Inject(renderHandsWithItems, RETURN, require=0)` → `renderAtHandFlush()`；26.1.2 字节码尾部 = `renderAllFeatures` + `endBatch`，Iris `HandRenderer.endRender` 从同一方法进入 → 钩子天然落在 Iris 手部阶段内 |
| `IrisCompat` | 版本感知 `isRenderShadow`（1.7.0 分界，`IrisCompatNewly/Legacy` 反射桥）+ `assignPipelineToIrisAny` 多候选 + `assignCommonEntityPipelinesToHandIfNeeded` 7 管线 once（HAND_CUTOUT 优先回退）+ `supportsHandFlushHook`/`assignMeshPipelineToHand`/`assignMeshPipelineToEntity`；`initCompat()` 接线到 `GunModClient` 构造器 |
| `CameraSetupEvent` | `applyScopeMagnification` 加 `suppressesWorldFovZoom(partialTick)` 分支：PIP 时按 `worldZoomAtProgress` 应用世界 share，`WORLD_FOV_DYNAMICS` 保持跟踪 |
| `PapiManager` | `I18n.get` → `Language.getInstance().getOrDefault`（纯查表，防 `%ammo_count%` 被 String.format 解析成 `Format error: ...`） |
| `ClientConfig` | `init(builder)` 追加 `MeshyConfig.init(builder)` |
| `RenderDistance` | `isGuiRender()` 改 public（`TaczPolyMeshGunModel.isWorldGpuContext` 消费）；窗口保持本仓历史 500ms（姊妹 26.1.2 是 100ms，两边基线早已分叉，本次只搬 public 化语义） |
| `ClientSetupEvent` | `onClientResourceReload` 加 `TaczMeshyIntegration.registerReloadListener(event::addListener)` |
| `ClientGameEvents` | `onScreenExtractPre/Post` 接 `ScreenRenderTracker.onScreenRenderPre/Post` |
| `GunModClient` | `IrisCompat.initCompat()` + `TaczMeshyIntegration.onClientSetup()` |

## 5. 均等检查（本仓跑通）

- `docs/check_mesh_config_parity.py`：**齐平 ✓**（toml 18 项 = Cloth 18 条 = 语言键 36 个，en/zh 一致；PIP 11 键另行核对：RenderConfig 11 define ↔ Cloth 11 条 ↔ 语言 22 键，默认值/范围一致）。
- `docs/check_mixin_registration.py`：**OK**（38 registered / 36 classes / 5 有意不注册）。
- `docs/check_lang_keys.py`：**OK**（310 个字面量键全部存在/白名单；脚本新增 `tacz.type.` 运行时拼接前缀白名单——`ClientGunTooltip` 用 `"tacz.type." + type + ".name"`）。
- 全库残留扫描：`net.fabricmc`/`cn.sh1rocu`/`EnvType`/`ForgeConfigSpec` 源码引用 = 0（注释除外）。

## 6. 未验证项（不要写 PASS）

- 全部新代码未编译（沙箱无 JDK；等 CI）。
- **Iris 光影下 mesh GPU / PIP 路径**：姊妹亦多处标注未实机（`MESH_LOADER.md` 复测矩阵、
  `GPU_WORLD_UNDER_SHADERS` 默认开的取舍）。本仓承接同一未验证状态。
- `MeshGpuUnderShaders`/`MeshGpuWorldUnderShaders` 的 Iris 1.11.x 版本门（`supportsHandFlushHook`）
  只做了源码级核对（Iris 26.1 分支 `MixinItemInHandRenderer` f4c0697），未在 26.1.2 实机。
- PIP 二次渲染（`ScopePipRerender`）的 26.1.2 `LevelRenderer.renderLevel` 注入点/签名只经
  姊妹字节码探针（`LevelRendererAccessor.submitNodeStorage` 字段名）交叉验证。
